#!/bin/bash -x
#PJM -N #BENCHNAME#
#NOTIFY_EMAIL#
#NOTIFICATION_TYPE#
#PJM -L node=#NODES#
#PJM --mpi proc=#TASKS#
#PJM_MPI_SHAPE#
#PJM -x PJM_LLIO_GFSCACHE=#GFSCACHE#
#PJM -L elapse=#TIME_LIMIT#
#PJM -L rscgrp=#RSCGROUP#
#PJM -o #STDOUTLOGFILE#
#PJM -e #STDERRLOGFILE#
#ACCOUNT_CONFIG#
#ADDITIONAL_JOB_CONFIG#

#ENV#

#PREPROCESS#

JUBE_ERR_CODE=$?
if [ $JUBE_ERR_CODE -ne 0 ]; then
    #FLAG_ERROR#
    exit $JUBE_ERR_CODE:x
fi

#MEASUREMENT# #STARTER# #ARGS_STARTER# #EXECUTABLE# #ARGS_EXECUTABLE#

JUBE_ERR_CODE=$?
if [ $JUBE_ERR_CODE -ne 0 ]; then
    #FLAG_ERROR#
    exit $JUBE_ERR_CODE
fi

#POSTPROCESS#

JUBE_ERR_CODE=$?
if [ $JUBE_ERR_CODE -ne 0 ]; then
    #FLAG_ERROR#
    exit $JUBE_ERR_CODE
fi

#FLAG#
